replicaCount: 1
image:
  repository: dockekroy/harness-java-demo
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  className: nginx
  host: harness-java-demo.local
  path: /
  pathType: Prefix

resources: {}
